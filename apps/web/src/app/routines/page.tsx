import { ViewShell } from '@/components/view-shell';

export default function RoutinesPage() {
  return (
    <ViewShell eyebrow="Build" title="Routines" subtitle="Daily habits and streaks — morning, afternoon, evening.">
      <div className="surface flex h-64 items-center justify-center text-sm text-muted-foreground">
        Routines view — being built.
      </div>
    </ViewShell>
  );
}
