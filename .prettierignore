# Generated by Next.js during dev/build. Its own header says it must not be
# edited, but Prettier does not read .gitignore, so leaving it unlisted makes
# `pnpm format:check` (and therefore `pnpm ci:local`) fail on any machine that
# has already run a build.
apps/web/next-env.d.ts

# Build output and caches.
**/.next/
**/dist/
**/out/
coverage/
playwright-report/
test-results/

# Lockfiles and vendored assets are managed by their own tooling.
pnpm-lock.yaml
